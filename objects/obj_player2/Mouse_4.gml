
originX = x;
originY = y;
//originList = movableLocations(originX, originY);
//position_origin = getMapLocation(originList[2], originList[3]);

if(tigerTurn == true){
	grabbed = true; // tiger grabbed
	xx = x - mouse_x; // amount of x space tiger move
	yy = y - mouse_y; //amount of y space tiger move
	goatTurn = false;

}